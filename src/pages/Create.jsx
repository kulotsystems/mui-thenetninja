import { Typography } from '@mui/material';

const Create = () => {

    return (
        <div>
            <Typography
                variant="h6"
                component="h2"
                gutterBottom
                color="textSecondary"
            >
                Create a New Note
            </Typography>
        </div>
    );
};

export default Create;
